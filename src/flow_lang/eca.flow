// Rule 30
@init(AAAAAAAAAAAAAAAAAAAAAAABAAAAAAAAAAAAAAAAAAAAAAA);

// define the rules
@compress(0);
@merge(0);
(-pl[inf] -mr[0,inf] -sr[0,2]) {
    // BB_ --> _A_;
    // BAA --> _B_;
    // AAB --> _B_;
    @decode(wns, AB, 126);
}

// Run n times
@evolve(20);
