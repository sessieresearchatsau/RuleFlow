// set the initial state
@Init(AB);

(-a -nib -nct -nds) {
    "All those Bs at the end of the sequence, but more than 3 Bs." ><;
}

// define the sequential rules
ABA -> AAB;
A -> ABA;

// Run n times
@Evolve(40);
