// Rule 30
@init(AAAAAAAAAAAAAAAAAAAABAAAAAAAAAAAAAAAAAAAA);

// define the rules
@merge(0);
//@compress(0);
(-pl[inf] -mr[0,inf]) {
    // BB_ --> _A_;
    // BAA --> _B_;
    // AAB --> _B_;
    @decode(wns, AB, 126);  // rule 30
}

// Run n times
@evolve(32);
