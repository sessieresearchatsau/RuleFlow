# initial state
@init("A" * 2 + "B" + 2 * "A");

# define the rules
@macro("stat.ca.preset");
@macro("stat.eca.pflow", "AB", 30);
[0] -> A;
[inf] -> A;

# run n times
# @p_seed(0);
#@clear();
@evolve(20);
